function exec() {
  sum(6, 3);
  subtract(6, 3);
  multiply(6, 3);
  divide(6, 3);
}

function sum(a, b) {
  alert(`Somma: ${a} + ${b} = ${a + b}`);
}

function subtract(a, b) {
  alert(`Somma: ${a} - ${b} = ${a - b}`);
}

function multiply(a, b) {
  alert(`Somma: ${a} * ${b} = ${a * b}`);
}

function divide(a, b) {
  alert(`Somma: ${a} / ${b} = ${a / b}`);
}
